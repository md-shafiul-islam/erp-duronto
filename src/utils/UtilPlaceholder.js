import React from 'react'

const UtilPlaceholder = ({text="", size="md", className=""})=> {
    return (
        <React.Fragment>
            Data Not found
        </React.Fragment>
    )
}

export default UtilPlaceholder;
